# AI 工牌 1.0 · 工作纪要（2026-08-21）

> 本纪要记录 2026-08-21 当日从「需求对齐」到「接口规范定稿」的完整工作内容、产出物、关键决策与遗留事项，以备后查。

---

## 一、今日目标

软件端（甲方）在硬件需求明确、ODM 尚在 BOM 选型的窗口期，完成「软件端顶层架构设计 → 端云接口规范 → 实现计划」的前期设计与对齐工作，为后续编码开工奠定基础。

## 二、完成的工作（按阶段）

1. **需求对齐**：通读《AI工牌1.0产品需求文档（0804）》与《软件需求及功能边界》，确认权威需求；发现目录内《技术架构·卷一~卷六》为 R818 时代旧文档、与 0804 根本冲突（主控 R818→ESP32 三芯片、双麦→四麦、墨水屏→彩屏、长按问答→独立问答键、单知识库→双知识库），判定**以 0804 为唯一权威**。

2. **架构设计**（superpowers brainstorming）：按「整体顶层架构总纲」推进，产出架构 spec，覆盖系统全景、模块边界、数据流、供应商抽象、TTS 打断、LID 软锁定、四层隔离、受控 RAG、错误码降级、配置/OTA 等。

3. **架构 review**：技术专家 + 产品专家双视角对抗 review 两轮，修复 3 个 P0、14 个 P1 及一批 P2，收敛到「无阻断项」。

4. **实现计划**（writing-plans）：将架构 spec 拆为 6 个可独立测试的 plan（P1–P6），每个含文件结构、接口签名、TDD 步骤、实际代码。

5. **端云接口规范**：产出《端云通信接口规范》，经 5 轮专家 review（初版→v2→v3→v4）收敛到「达 ODM 联调标准」；并同步 P3/P4 plan 与架构 spec。

## 三、产出物清单

| 类别 | 文件路径 | 规模 |
|---|---|---|
| 需求基线 | `docs/requirements/PRD-0804.txt` | 1007 行 |
| 需求边界 | `docs/requirements/software-boundary.txt` | 418 行 |
| **架构 spec** | `docs/superpowers/specs/2026-08-21-ai-card-software-architecture-design.md` | 651 行 |
| **接口规范 v4** | `docs/端云通信接口规范-v1.md`（内容为 v4） | 440 行 |
| P1 骨架+隔离 | `docs/superpowers/plans/2026-08-21-p1-project-skeleton.md` | 990 行 |
| P2 供应商抽象 | `docs/superpowers/plans/2026-08-21-p2-provider-abstraction.md` | 586 行 |
| P3 网关 | `docs/superpowers/plans/2026-08-21-p3-gateway.md` | 1019 行 |
| P4 翻译链路 | `docs/superpowers/plans/2026-08-21-p4-translation-proxy.md` | 767 行 |
| P5 知识库 | `docs/superpowers/plans/2026-08-21-p5-skb-rag.md` | 731 行 |
| P6 旁路+后台 | `docs/superpowers/plans/2026-08-21-p6-ingestion-admin.md` | 509 行 |

## 四、关键决策

| 决策点 | 结论 |
|---|---|
| 权威需求 | 0804 PRD（旧 R818 文档作废，仅提取通用理念） |
| 技术栈 | Java 17 + Spring Boot（模块化单体起步） |
| 市场/部署 | 日本（与供应商同 region） |
| 翻译链路 | **云端薄代理**（LID 独立前置 → ASR/MT/TTS） |
| 供应商 | Google + Azure，多供应商抽象 + 失败降级 |
| 知识库 | skb 受控 RAG（pgvector + Azure OpenAI）；**vkb 游客知识库（已实现，见 docs/VKB游客FAQ-日语版.md）** |
| 向量库 | pgvector（时延最短 + 稳定 + 私有化友好） |
| 音频传输 | 二进制帧 + JSON 控制（非 base64） |
| 服务拆分 | 模块化单体（未来可拆 gateway/translation） |
| 旁路采集 | 薄层 tee 脱敏文本；原始音频 demo 默认关闭 |

## 五、Review 收敛历史

| 对象 | 轮次 | 结果 |
|---|---|---|
| 架构 spec | 2 轮（技术+产品专家） | 3 P0 + 14 P1 + P2 全部清零 |
| 接口规范 | 5 轮（初版→v2→v3→v4） | 收敛到「无 P0/P1，达 ODM 联调标准」 |

**Review 抓到的关键问题（单人设计易漏）**：
- 翻译主链路缺「员工→游客」方向（方向应由 source_side 决定，非 LID 语言）
- LID 软锁定的置信度在「一步 speech-to-speech」下拿不到 → LID 独立前置
- 员工问答若做成新 session 会清 visitor_language → scope 与 translation_mode 拆分 + 键状态迁移表

## 六、⚠️ 遗留事项（需跟进，非设计产物可自动解决）

1. ~~【必须】VKB 后置的 PRD 书面偏离记录~~ **✅ 已解决（2026-09）**——vkb 已改为 1.0 要做并实现：游客提问翻译成日语后撞 VKB，命中则嘀嘀提示服务人员，按 OK 确认后答案反向翻译成游客语言播放（见《端云通信接口规范》§5.1 + docs/VKB游客FAQ-日语版.md）。
2. **【联调前】待冻结参数**——采样率（建议 16k）、编码（PCM16 单声道）、帧长（100ms）、心跳（10s）、LID 阈值（0.8/0.95）、背压流控等，需 ODM 硬件能力 + EVT 验证后冻结。
3. **【联调前】真实供应商适配**——Azure/Google 真实 API 对接、LID 置信度可获取性实测、embedding/LLM（Azure OpenAI）选型。
4. **【开发前】环境准备**——项目尚未 `git init`；执行 plan 需本地 Docker（Testcontainers 跑 PostgreSQL/pgvector）。
5. **【可选】LID 策略配置项补全**——第三语言处理、软锁定连续 N 次等后台配置参数。

## 七、下一步

两条路径（待定）：
1. **开始执行开发**：按 P1 → P2/P3 → P4 → P5 → P6 顺序，subagent-driven 或 inline，先 git init + 准备 Docker。
2. **先补遗留项**：落实 VKB 偏离记录 + 与 ODM 对齐待冻结参数，再启动开发。

---

*本文档由 Claude Code 生成，作为当日工作记录存档。*
