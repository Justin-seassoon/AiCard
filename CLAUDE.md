# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> ⚠️ 当前状态：本仓库尚无源码、无 git 仓库、无构建系统。目录内仅有产品需求文档（PRD）与参考图。
> 本文件记录的是 **产品上下文**，用于在编码开工前帮助后续会话理解项目意图与约束。
> 一旦开始落地代码，请补充实际的构建 / lint / 测试命令与代码架构。

## 项目概述

**AI 工牌 1.0**：一款面向日本市场服务业的可佩戴式 AI 语音翻译终端（胸牌/工牌形态），部署在酒店前台、免税店柜台、机场服务台、展会接待台等室内高网络覆盖场景，帮助店员与游客进行多语种双向语音沟通。

权威需求来源：`AI工牌1.0产品需求文档（0528）.docx`（如需阅读，用 `textutil -convert txt "AI工牌1.0产品需求文档（0528）.docx" -stdout`）。

## 核心设计原则

1.0 阶段坚持 **「硬件轻量、云端翻译、交互简单、快速量产」**：
- **端侧设备只做**：音频采集、语音播放、网络连接、按键交互、墨水屏低频显示、低功耗管理、日志、基础 OTA。
- **云端中台做**：语种判定（LID）、ASR/MT/TTS、第三方 API 路由、配置下发、设备管理、运营后台。
- **不自研模型**：1.0 优先购买成熟 API（科大讯飞 / 火山引擎 / Google / 腾讯），且至少预留 2 家供应商以防锁定。
- **不从零开发安卓大板**：优先采用 ODM 成熟联网音频 PCBA 方案。

## 系统架构（目标参考架构）

整体是「联网音频终端 + 云端翻译中台 + 低频设置屏」的组合，**不是带屏聊天翻译机**。

```
Device Badge（工牌）
  | 会话级短时流式音频通道（不要求长连接）
API Gateway / 设备接入层（鉴权：device_id + token + firmware_version）
  |
Session Manager + Device Manager（在线/睡眠/离线状态）
  |
Middleware Router
  |-- LID（在选定语言对内做语种判定）
  |-- ASR Provider: iFlytek / Volcengine / Google / Tencent
  |-- MT  Provider: Google / Tencent / Volcengine / iFlytek
  |-- TTS Provider: iFlytek / Google / Volcengine / Tencent
  |-- 可选 KB / 场景技能库
  |
TTS 音频回包 -> 设备扬声器外放
```

三大模块边界（这是理解全局的关键，需读多处需求才能串起来）：

- **端侧固件**（PRD §7）：连续对话状态机、VAD 断句、音频分片上传、TTS 播放、按键/墨水屏/LED、低功耗。业务逻辑尽量不放端侧。
- **云端 Middleware**（PRD §8）：项目核心软件层。设备接入、会话管理、LID + 降级策略、多供应商 API 路由、配置下发、运营后台、安全合规。
- **H5 / 后台**（PRD §9）：一机一码扫码配置、默认语言对、低频语言菜单、LID 策略、设备绑定、权限隔离。配置经 MQTT 或等价轻量通道下发到设备。

## 关键交互模型（容易踩坑，务必理解）

- **连续对话模式**：短按一次翻译键开启会话级流式通道；会话期间 VAD 自动断句、连续轮流对话，**中途无需每句重复按键**；再次短按或 **连续 20 秒无有效语音/按键** 自动结束并进入低功耗。定位为「连续轮流对话」，不承诺电话级双向同时讲话；播 TTS 时需有基础 AEC / 采播互斥，避免回采啸叫。
- **无方向键**：1.0 取消「员工键/游客键」。翻译方向由「当前语言对 + 云端 LID 自动判定」共同决定。
- **高频 vs 低频语言**：高频语言对（中日 / 英日 / 韩日）用实体语言键循环切换；低频小语种（泰/越/法/西/印尼/马来等 ⇄ 日）通过背面墨水屏菜单或 H5/后台配置。
- **LID 降级是硬约束**：置信度阈值建议初始 `Confidence >= 0.8`；低于阈值**不得直接外放高风险译文**，须降级（提示重说 / 按门店默认方向 / 短句白名单如 OK、ありがとう、はい），并记录低置信度日志。

## 关键验收指标（实现时的目标值，PRD §10）

- 端到端响应延迟：目标 < 1.5s（理想接近 1s），稳定 Wi-Fi 下 100 轮短句实测。
- 墨水屏菜单局部刷新（Partial Refresh）：上/下键切换目标响应 <= 200ms；全局刷新仅在进出菜单/确认/清残影时触发。
- VAD 静音断句：人声停止约 1.2s 触发尾包（end_of_utterance）。
- 20 秒自动熔断、待机 7–10 天（Deep Sleep）、收音距离 0.5–1.5m。

## 音频流协议要点（PRD §7.2）

- 不要求长期常连接：翻译触发后按需建立会话级短时流式连接，空闲超时断开；睡眠前上报 `sleep_notice`。
- 音频包字段：`session_id` / `sequence` / `timestamp` / `audio_payload`；尾包带 `end_of_utterance`。
- 返回包类型：`partial_text` / `final_text` / `tts_audio` / `error` / `config_update`。**设备端只播 TTS，不显示翻译文本。**
- 采样率待评估 16/24/48kHz；编码格式由云端协议确定。
- 第三方 API Key **不得写入端侧固件**，必须云端统一管理。

## 候选硬件平台

- 主控：P0 全志 R818（或同等低功耗 Linux 音频平台）；P1 ESP32-S3（或同等 RTOS，成本优先）。
- 双 MEMS 硅麦 + 基础降噪/VAD/AEC；内置扬声器 + 功放外放。
- 背面低功耗墨水屏：优先柔性/塑料基材（避免普通玻璃基材碎屏风险）；显示语言对/电量/网络/设备编号/二维码/小语种菜单，不显示翻译文字。
- 通信：双频 Wi-Fi（日本版）+ 可选 4G Cat.1；FPC 天线；BLE 仅预留配网/维护。
- 供电：~1000mAh 超薄电池，Type-C 5V/1A 慢充。

## 后续编码开工时需补充本文件的内容

- 实际仓库结构（端侧固件 / 云端 Middleware / H5 后台 通常会分仓或分目录）。
- 构建、lint、测试、单测运行命令（按所选语言/框架填写）。
- 音频流协议、连续对话状态机的具体实现文档位置。
